import React from 'react';
import store from "../store";

function ShowModal(props) {
  return (
    <div>
      <button onClick={
        () => {

        }
      }>Show Modal</button>

    </div>
  );
}
export default ShowModal;